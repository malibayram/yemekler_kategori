import 'package:flutter/material.dart';

class AnaYemekler extends StatelessWidget {
  const AnaYemekler({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("AnaYemekler"),
    );
  }
}
